import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, configured } from './config.js';
const byId=id=>document.getElementById(id);const supabase=configured?createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY):null;if(!configured)byId('configWarning').classList.remove('hidden');
function mode(value){const register=value==='register';byId('loginForm').classList.toggle('hidden',register);byId('registerForm').classList.toggle('hidden',!register);byId('loginTab').classList.toggle('active',!register);byId('registerTab').classList.toggle('active',register);byId('authTitle').textContent=register?'Davetinizle başlayın.':'Tekrar hoş geldiniz.';byId('authSubtitle').textContent=register?'Atlas erişiminizi birkaç saniyede oluşturun.':'Lisanslarınıza ve güvenli indirme alanına erişin.';history.replaceState(null,'',register?'?mode=register':'auth.html')}
function status(id,message,ok=false){const el=byId(id);el.textContent=message;el.className=`form-status show ${ok?'ok':'error'}`}
byId('loginTab').addEventListener('click',()=>mode('login'));byId('registerTab').addEventListener('click',()=>mode('register'));if(new URLSearchParams(location.search).get('mode')==='register')mode('register');
if(supabase){const{data}=await supabase.auth.getSession();if(data.session)location.replace('dashboard.html')}
byId('loginForm').addEventListener('submit',async event=>{event.preventDefault();if(!supabase)return status('loginStatus','Supabase bağlantısı yapılandırılmadı.');const button=event.submitter;button.disabled=true;const{error}=await supabase.auth.signInWithPassword({email:byId('loginEmail').value.trim(),password:byId('loginPassword').value});button.disabled=false;if(error)return status('loginStatus','E-posta veya şifre hatalı.');location.replace('dashboard.html')});
byId('registerForm').addEventListener('submit',async event=>{event.preventDefault();if(!configured)return status('registerStatus','Supabase bağlantısı yapılandırılmadı.');const button=event.submitter;button.disabled=true;try{const response=await fetch(`${SUPABASE_URL}/functions/v1/register`,{method:'POST',headers:{'Content-Type':'application/json',apikey:SUPABASE_PUBLISHABLE_KEY},body:JSON.stringify({username:byId('registerUsername').value.trim(),email:byId('registerEmail').value.trim(),password:byId('registerPassword').value,inviteKey:byId('inviteKey').value.trim()})});const result=await response.json();if(!response.ok)throw new Error(result.error||'Kayıt başarısız.');status('registerStatus',result.message,true);byId('loginEmail').value=byId('registerEmail').value.trim();setTimeout(()=>mode('login'),900)}catch(error){status('registerStatus',error.message)}finally{button.disabled=false}});
